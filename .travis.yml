language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-fumunda' ]; then cd .. && eval "mv $currentfolder generator-fumunda" && cd generator-fumunda; fi

